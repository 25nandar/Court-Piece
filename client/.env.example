# URL of the realtime server. For local dev the default (http://localhost:3001)
# is used automatically, so you only need this for production builds.
# On Vercel, set this to your Render server URL, e.g.:
#   VITE_SERVER_URL=https://court-piece-server.onrender.com
VITE_SERVER_URL=http://localhost:3001
